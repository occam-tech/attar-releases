# Attar developer packages

Public distribution of versioned Attar developer packages.

Linux preview publication is being prepared. Packages target x86_64 Linux with glibc 2.39 or newer.

Apple Silicon packages target macOS 15 or newer; validation takes place on a separate Mac.

Release assets contain the developer tools and native SDK. This repository contains distribution metadata; the Attar source repository remains private.
